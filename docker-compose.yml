version: '3'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db

  db:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: hilel_task_db
      POSTGRES_USER: username
      POSTGRES_PASSWORD: password